# Shared Rune plugin Python dependencies
# All scripts use a single venv at scripts/.venv/
# Setup: session-start.sh (SessionStart hook)
PyYAML>=6.0
mcp[cli]>=1.6.0,<2.0.0
httpx>=0.27.0
pydantic>=2.0
defusedxml>=0.7.1
pytest-asyncio>=0.23.0
